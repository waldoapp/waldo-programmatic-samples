describe('Wikipedia Android - Tutorial', () => {
  it('navigates the app', async () => {
    await driver.tapElement('text', 'SKIP');

    // Follow the tutorial at https://app.waldo.com/applications/ios/sessions?tutorial=true
    // and learn how to extend this script
  });
});
