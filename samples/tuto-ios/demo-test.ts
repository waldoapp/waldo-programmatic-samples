describe('Wikipedia iOS - Tutorial', () => {
  it('navigates the app', async () => {
    await driver.tapElement('text', 'Skip');

    // Follow the tutorial at https://app.waldo.com/applications/android/sessions?tutorial=true
    // and learn how to extend this script
  });
});
